export type TVideo = {
    id: string,
    title: string,
    durSeconds: string,
    dateUpload: string
}

export interface videoDB {
    id: string,
    title: string,
    dur_seconds: number,
    date_upload: string
}