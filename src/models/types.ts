export interface VideoType  {
    id: string,
    title: string,
    durSeconds: number,
    dateUpload: string
}

export interface VideoDB {
    id: string,
    title: string,
    dur_seconds: number,
    date_upload: string
}